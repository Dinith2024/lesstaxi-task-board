{
  "name": "task-board-backend",
  "version": "1.0.0",
  "description": "REST API for a Trello-like task board (auth, roles, tasks) built on Node core modules only.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node scripts/seedAdmin.js"
  },
  "dependencies": {},
  "license": "MIT"
}